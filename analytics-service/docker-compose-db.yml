version: "3.9"

services:
  analytics-service-db:
    container_name: analytics-service-db
    image: postgres:latest
    environment:
      POSTGRES_USER: admin_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: db
    ports:
      - "5003:5432"
    volumes:
      - "D:/db_volumes/anlytics-service-db:/var/lib/postgresql"
    networks:
      - internal

networks:
  internal:
    external: true

